name: gym-cors
version: 17.0.0
patches:
  caddyfile-lms: |
    header * {
      # Content-Security-Policy "frame-ancestors 'self' {{ LMS_HOST }} *.{{ LMS_HOST }} {{ MARKETING_SITE_BASE_URL }};
      # connect-src {{ LMS_HOST }} *.{{ LMS_HOST }} {{ MARKETING_SITE_BASE_URL }};
      # script-src {{ LMS_HOST }} *.{{ LMS_HOST }} {{ MARKETING_SITE_BASE_URL }};
      # worker-src {{ LMS_HOST }} *.{{ LMS_HOST }} {{ MARKETING_SITE_BASE_URL }};
      "
    }