name: gym-cors
version: 17.0.1
patches:
  caddyfile-lms: |
    header * {
      # Content-Security-Policy "frame-ancestors 'self' {{ LMS_HOST }} *.{{ LMS_HOST }} {{ MARKETING_SITE_BASE_URL }} gym.soy *.gym.soy;
      # connect-src {{ LMS_HOST }} *.{{ LMS_HOST }} {{ MARKETING_SITE_BASE_URL }} gym.soy *.gym.soy;
      # script-src {{ LMS_HOST }} *.{{ LMS_HOST }} {{ MARKETING_SITE_BASE_URL }} gym.soy *.gym.soy;
      # worker-src {{ LMS_HOST }} *.{{ LMS_HOST }} {{ MARKETING_SITE_BASE_URL }} gym.soy *.gym.soy;
      "
    }